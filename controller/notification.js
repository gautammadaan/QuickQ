/**
* @param users, callback
* send push notifications to users to answer questions
**/
function notifyUsers(users, callback) {
     // loop over users to get device Ids

     // send http request to notify users with above device ID
}

module.exports = {
    notifyUsers: notifyUsers
}